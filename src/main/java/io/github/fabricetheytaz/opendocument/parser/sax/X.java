package io.github.fabricetheytaz.opendocument.parser.sax;

public class X
	{
	/*
	public static void sd() throws IOException
		{
		try (final IOpenDocument document = new OpenDocumentText(null))
			{
			try (final Writer writer = new StringWriter())
				{
				final W converter = new W(writer);

				parse(document, converter);

				System.out.println(converter.toString());
				}
			}
		}
	*/
	}
