package io.github.fabricetheytaz.opendocument.parser.dom;

public class Parser
	{
	/*
	public static void w()
		{
		try (final UncheckedIOExceptionWriter writer = new UncheckedIOExceptionWriter(new StringWriter()))
			{
			writer.write("Hello");
			writer.write(" ");
			writer.write("World");
			System.out.println(writer);
			}

		final DocumentBuilderFactory factory = DocumentBuilderFactory.newDefaultInstance();
		factory.setNamespaceAware(true);
		final DocumentBuilder builder = factory.newDocumentBuilder();
		builder.parse(new InputSource(new StringReader("dsd")));
		final Document document = builder.newDocument();
		final Element element = document.createElementNS("sddd", "sdsd");
		document.getDocumentElement().appendChild(element);
		}

	public static Document parse(final String xml) throws IOException, ParserConfigurationException, SAXException
		{
		final DocumentBuilderFactory factory = DocumentBuilderFactory.newDefaultInstance();

		factory.setNamespaceAware(true);

		final DocumentBuilder builder = factory.newDocumentBuilder();

		try (final Reader reader = new StringReader(xml))
			{
			return builder.parse(new InputSource(reader));
			}
		}
	*/
	}
