package io.github.fabricetheytaz.opendocument.parser.dom;

/**
 * @version 0.1.0
 * @since 0.1.0
 */
public class Show //extends AbstractNodeVisitor
	{
	/*
	@Override
	public void startElement(final Element element, final int level)
		{
		for (int i = 0; i < level; i++)
			{
			System.out.print("  ");
			}

		System.out.println(element.getLocalName() + " - " + element.getNamespaceURI());
		}

	@Override
	public void startText(final Text text, final int level)
		{
		for (int i = 0; i < level; i++)
			{
			System.out.print("  ");
			}

		System.out.println(text.getTextContent());
		}
	*/
	}
